/*

export function synchronizer(num:number):string{

  switch(num){
    case  % 3:
      console.log(`balling`);
      return;
    case "Saturday":
      console.log(`${num} is for weddings`);
      return;
    case "Sunday":
      console.log(`${num} is for worship`);
      return;
    default:
      console.log(`${num} is for work`);
  }
  
}

*/